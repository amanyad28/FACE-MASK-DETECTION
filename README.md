# FACE-MASK-DETECTION
About Implementing a face mask detection model using a convolutional neural network. The model is trained on a dataset of images with and without masks, and can classify whether a person in an input image is wearing a mask or not. The code includes data preprocessing, model training, and prediction on
